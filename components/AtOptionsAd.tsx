import React, { memo } from 'react';

// Adsterra ads temporarily disabled
interface AtOptionsAdProps {
  className?: string;
  label?: string;
}

const AtOptionsAd = (_props: AtOptionsAdProps) => null;

export default memo(AtOptionsAd);
