import React, { memo } from 'react';

// Adsterra ads temporarily disabled
interface AdsterraAdProps {
  className?: string;
  label?: string;
}

const AdsterraAd = (_props: AdsterraAdProps) => null;

export default memo(AdsterraAd);
